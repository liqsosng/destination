import RenderDestination from "../features/destinations/RenderDestination";

const InfoScreen = (props) => {
  return <RenderDestination destination={props.destination} />;
};

export default InfoScreen;
